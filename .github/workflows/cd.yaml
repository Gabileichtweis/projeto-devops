name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Simulação de Deploy
        run: |
          echo "Aplicação aprovada na CI"
          echo "Iniciando deploy..."
          echo "Deploy concluído!"
          